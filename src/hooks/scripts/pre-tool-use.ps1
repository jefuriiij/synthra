# PreToolUse hook — Windows PowerShell.
# THE MOAT (improvement #1). POSTs the tool call to /gate; if response is
# "block", exits 2 with the reason (Claude treats exit 2 as deny).
# TODO: M3

Write-Error "[syn] pre-tool-use.ps1 not yet implemented (M3)"
exit 0
