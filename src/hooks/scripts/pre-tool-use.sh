#!/usr/bin/env bash
# PreToolUse hook — macOS/Linux.
# THE MOAT (improvement #1). POSTs the tool call to /gate; if response is
# "block", exits 2 with the reason.
# TODO: M3

echo "[syn] pre-tool-use.sh not yet implemented (M3)" >&2
exit 0
