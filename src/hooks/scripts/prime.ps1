# SessionStart hook — Windows PowerShell.
# Calls GET /prime on the local MCP server, prints the primer to stdout.
# TODO: M3 — real implementation. Reference: docs/PROTOCOL.md

Write-Error "[syn] prime.ps1 not yet implemented (M3)"
