// Extracts function/class signatures from a FileNode (no bodies).
// TODO: M2

import type { FileNode } from "../graph/types.js";

export function extractSignatures(_f: FileNode): string {
  throw new Error("Synthra: extractSignatures not yet implemented (M2)");
}
