// Per-file keyword extraction. Used for query-time relevance ranking.
// Approach: tokenize identifiers + comments, filter stopwords, prefer rare tokens.
// TODO: M1

export function extractKeywords(_content: string, _ext: string): string[] {
  throw new Error("Synthra: extractKeywords not yet implemented (M1)");
}
