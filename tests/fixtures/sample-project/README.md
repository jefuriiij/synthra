# sample-project (test fixture)

A small synthetic project used by Synthra's scanner/packer/rank tests.

To be populated alongside M1 — should include:
- A few TypeScript files with imports between them
- A Python file
- A Svelte component
- A test file matching one of the sources
- A `.gitignore` that excludes a dir, to verify the walker
