// Ranking tests.
// TODO: M2

import { describe, it, expect } from "vitest";

describe("rank", () => {
  it.todo("ranks files by keyword overlap with query");
  it.todo("boosts files reachable from session-known files via imports");
  it.todo("boosts recently-edited files");
});
