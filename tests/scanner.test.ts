// Scanner tests against tests/fixtures/sample-project.
// TODO: M1

import { describe, it, expect } from "vitest";

describe("scanner", () => {
  it.todo("walks the sample fixture and yields all .ts files");
  it.todo("respects .gitignore");
  it.todo("respects .synthraignore");
  it.todo("extracts symbols + imports from a TypeScript file");
});
